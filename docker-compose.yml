services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./workspaces:/app/workspaces
  sandbox:
    build:
      context: ./sandbox
    image: codenex-sandbox:latest
