# CodeNex Sandbox

This container executes generated project code in isolation from the backend container.
It is intentionally separate so untrusted code is never executed directly on the backend host process.
